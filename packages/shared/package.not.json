{
  "name": "@cwellsx/shared",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b --watch",
    "clean": "rimraf dist",
    "prepublishOnly": "echo \"This package is private\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
